# 上传目录恶意文件排查与清理知识梳理（安全版）

> 本文档用于记录用户上传目录、静态资源目录中恶意文件的识别、清理与防护思路。
>
> 为避免被杀毒软件误判，本文不保存真实木马代码、完整攻击载荷或可直接复用的危险片段。涉及恶意特征时均使用“去武器化”描述。

---

## 1. 背景与风险

对外可访问的静态资源目录，例如 `public/`、`uploads/`、`static/`，通常会保存用户上传的图片、附件、二维码、表格等文件。

如果上传接口只校验文件后缀、浏览器传来的 `Content-Type`，或完全不校验真实文件内容，攻击者就可能把脚本木马、图片马、漏洞探测载荷伪装成普通图片上传。

主要风险包括：

- 上传目录被写入可执行脚本，配合服务器解析配置不当形成 Webshell。
- 伪装图片中混入脚本片段，配合文件包含、错误的静态目录配置或历史中间件漏洞执行。
- 扫描器批量上传探针文件，用于判断服务是否存在 SQL 注入、路径穿越、模板注入、命令注入等漏洞。
- 上传目录长期堆积恶意样本，后续迁移或部署到其他运行环境时可能变成真实执行风险。

即使当前项目使用 NestJS 仅做静态托管，不解析 PHP/JSP/ASP，这类文件仍说明上传入口曾被扫描或滥用，应尽快清理并加固。

---

## 2. 常见恶意文件类型

| 类型 | 表现 | 风险 |
|------|------|------|
| Webshell | 脚本文件接收请求参数并执行命令或加载远程代码 | 可被远程控制服务器 |
| JSP/PHP/ASP 探针 | 返回固定哈希、执行简单表达式、回显环境信息 | 用于确认脚本是否能执行 |
| 图片马 | 文件头看似图片，文件内容中混入脚本片段 | 配合文件包含或错误解析执行 |
| SQL 注入探针 | 文件内容中包含延时、报错、布尔判断类 SQL 片段 | 判断后端是否拼接 SQL |
| 路径穿越探针 | 内容中包含读取系统敏感文件的路径模式 | 判断是否存在任意文件读取 |
| 命令注入探针 | 内容中包含 shell 表达式、Windows 命令表达式 | 判断是否拼接命令执行 |
| 模板注入探针 | 内容中包含模板表达式或模板变量计算 | 判断模板引擎是否解析用户输入 |
| CRLF 探针 | 内容中包含换行注入与伪造 HTTP 头 | 判断响应头是否可被污染 |
| 接口扫描命名 | 文件名像 `actuator`、`env`、`gateway`、`swagger` 等 | 常见自动化扫描痕迹 |
| 极小假图 | 0 字节、1 字节或明显过小的“图片” | 多为上传接口测试垃圾 |

---

## 3. 识别思路

恶意文件排查不能只看扩展名，应综合扩展名、文件头、内容特征、大小、命名规则一起判断。

### 3.1 扩展名检查

重点关注这些类别：

- 服务端脚本：PHP、JSP、ASP、ASPX、CGI、Perl、Python、Ruby、Lua 等。
- 命令脚本：Shell、Bash、Batch、CMD、PowerShell、VBScript 等。
- 可执行文件：EXE、DLL、SO、BIN、MSI、SCR、COM、JAR、WAR、CLASS 等。
- 可嵌入脚本的文本格式：HTML、SVG、XML、JS 等。
- Web 服务器配置：`.htaccess`、`.htpasswd`、类似服务器配置文件。

还要注意绕过方式：

- 大小写混用。
- 双扩展，例如“图片后缀 + 脚本后缀”或“脚本后缀 + 图片后缀”。
- 尾部点、空格、特殊流名称。
- URL 编码后的点和斜杠。
- 分号截断或历史服务器解析差异。

### 3.2 文件魔数校验

文件魔数是文件开头的一组固定字节，比扩展名更可信。

常见类型：

| 类型 | 文件头特征 | 说明 |
|------|------------|------|
| JPEG | 以 JPEG 固定头开始 | 普通照片类图片 |
| PNG | 以 PNG 固定头开始 | 常见无损图片 |
| GIF | 以 GIF 固定头开始 | 动图或兼容图片 |
| WebP | 以 RIFF 容器并声明 WebP | 现代图片格式 |
| BMP | 以 BMP 固定头开始 | 位图 |
| ZIP / 新版 Office | 以 ZIP 容器头开始 | 也可能是 jar、docx、xlsx |
| 旧版 Office | 以 OLE 复合文档头开始 | 例如旧 xls |
| Windows 可执行 | 以 Windows PE 固定头开始 | exe、dll |
| Linux 可执行 | 以 ELF 固定头开始 | Linux 可执行文件 |
| 文本/标记文件 | 以 `<`、脚本 shebang 或可读文本开始 | 可能是 HTML、XML、SVG、脚本 |

判断方式：

1. 读取文件前几十个字节。
2. 判断真实类型。
3. 和扩展名进行比对。
4. 如果扩展名是图片，但文件头不是图片，应进入人工复核。
5. 如果文件头是图片，但文件中还混有服务端脚本标记，也要视为高危。

注意：扩展名和魔数不一致不一定都是恶意。例如历史上传中可能出现实际是 WebP、但后缀被保存成 `.jpg` 或 `.gif` 的情况。这类文件要结合内容扫描再判断，不能直接批量删除。

### 3.3 内容特征检查

内容扫描适合用于小文件，以及大文件的头部和尾部片段。

建议重点查找以下类别的特征，但实际文档和脚本中应使用“去武器化”或哈希化规则，避免杀毒软件误报：

- 服务端脚本起始标记。
- 接收请求参数并执行的危险函数。
- 编码、解码、动态加载、反射执行相关调用。
- 系统命令执行相关调用。
- 常见系统敏感路径。
- SQL 延时、报错、布尔探测语句。
- 模板表达式计算片段。
- HTTP 换行注入标记。
- 已知 Webshell 工具的固定握手、固定口令或固定探针字符串。

推荐把检测规则放在扫描脚本中，而不是完整写入普通 Markdown 文档中。如果需要记录，应使用描述性文字，例如“PHP 起始标记”“JSP 页面指令”“延时 SQL 函数”，不要保存完整可匹配样本。

### 3.4 文件大小和命名规则

可疑信号：

- 图片文件大小为 0 字节、1 字节，或远小于正常业务图片大小。
- 文件名与业务规则不一致。例如本应是 UUID，却出现接口名、后台路径名、配置名。
- 二维码目录本应是纯数字编号，却出现 `env`、`actuator`、`gateway`、`swagger` 一类扫描字样。
- 同一时间批量出现大量小文件，且内容像探针。
- 文件修改时间集中在异常时段。

---

## 4. 推荐排查流程

1. 统计目录总文件数和扩展名分布。
2. 找出危险扩展名和异常命名文件。
3. 对所有文件读取魔数，标记真实类型与后缀不一致的文件。
4. 对小文件和可疑文件做内容特征扫描。
5. 对业务目录规则做校验，例如二维码是否都是数字编号，上传图片是否都是 UUID。
6. 对可疑清单人工复核。
7. 确认恶意后删除或隔离。
8. 重新全量扫描，确认无残留。

排查阶段建议只读，不要边扫边删。先形成清单，再统一处理。

---

## 5. 本项目本次发现的问题

本项目 `public` 目录中曾发现多类恶意或探测残留，已清理。

| 位置 | 数量 | 说明 |
|------|------|------|
| `public/files/2023-06-26/` | 99 | 扫描器批量上传的探测载荷，包括脚本探针、图片马、SQL 注入探针、路径穿越探针、模板注入探针等 |
| `public/files/2022-12-14/` | 2 | 一个 JSP 类 Webshell 探针和一个 1 字节假图 |
| `public/files/2022-12-08/` | 1 | 1 字节假图 |
| `public/trees/qrcodes/` | 29 | 非业务命名二维码，名称疑似接口扫描探测 |

合计删除 131 个文件。

复扫结果：

- 危险扩展名：0。
- 脚本 / Webshell 内容特征：0。
- 注入 / 路径穿越 / 模板注入探针：0。
- 异常二维码命名：0。
- 历史攻击目录已不存在。

---

## 6. 清理原则

### 6.1 删除前

- 如果需要溯源，先将样本移动到不可访问的隔离目录，并记录文件名、大小、时间、哈希。
- 不要把样本压缩包放在静态目录里。
- 不要直接打开可疑 HTML、SVG、脚本或 Office 文件。

### 6.2 删除时

- 使用精确路径删除，避免通配符误删。
- 对确认整目录都是攻击样本的目录，可以删除目录。
- 保留业务正常图片、表格和占位 `.gitkeep`。

### 6.3 删除后

- 重新扫描全目录。
- 检查是否还有危险扩展名。
- 检查是否还有异常命名文件。
- 检查是否还有脚本/探针内容特征。
- 如果近期仍继续出现新恶意文件，说明上传接口仍未修复或仍在被扫描。

---

## 7. 后续加固建议

### 7.1 上传接口加固

- 只允许业务需要的文件类型。
- 同时校验扩展名、MIME、魔数，其中魔数最关键。
- 使用服务端生成的文件名，不保留用户原始文件名。
- 保存文件时使用受控扩展名。
- 限制文件大小、数量、上传频率。
- 拒绝 0 字节文件和明显异常小的文件。
- 对图片做二次解码和重新编码，去掉潜在隐藏内容。
- 上传完成后异步扫描，命中风险则删除并告警。

### 7.2 静态目录加固

- 静态目录禁止解析任何脚本。
- 不要把上传目录和代码目录混在一起。
- 上传目录尽量使用独立存储桶或独立磁盘。
- 配置响应头，避免浏览器把上传文件当 HTML 或脚本执行。
- 对静态目录关闭目录列表。

### 7.3 运维与监控

- 定时扫描上传目录。
- 命中高危扩展名或脚本特征时告警。
- 记录上传来源 IP、用户、接口、文件大小和保存路径。
- 保留 Nginx / 应用访问日志，方便回溯攻击入口。
- 对异常高频上传、批量小文件上传做限流或封禁。

---

## 8. 常见误报

### 8.1 WebP 被保存成 jpg/gif

本项目复扫时发现部分 `.jpg` 或 `.gif` 实际文件头是 WebP。这种情况通常是历史上传或处理链路保存扩展名不准确，不一定是恶意。

处理建议：

- 如果内容没有脚本特征，且能被图片库正常解码，可以保留。
- 后续可统一转码或修正扩展名。
- 不要仅凭“扩展名和魔数不一致”批量删除。

### 8.2 合法小图

极小 PNG 可能是合法占位图或极简图标。判断时要结合魔数、内容和业务路径。

### 8.3 文档误报

安全文档如果直接保存真实木马片段、完整危险函数调用、攻击载荷字符串，可能被杀毒软件当作木马样本删除。

建议：

- 用描述性文字代替真实代码。
- 示例使用占位符。
- 不保存完整 webshell。
- 不保存可直接复制执行的攻击语句。
- 扫描规则放在专用脚本或安全平台中，文档中只写规则类别。

---

## 9. 简化扫描伪代码

以下仅描述流程，不包含真实恶意特征：

```text
遍历 public 目录下所有文件：
  读取文件名、扩展名、大小、修改时间
  如果扩展名属于危险类型：
    标记为高危

  读取文件头字节：
    判断真实文件类型
    如果后缀和真实类型冲突：
      加入可疑清单

  对小文件和可疑文件读取文本内容：
    如果包含脚本执行、命令执行、注入探针、路径穿越等特征：
      标记为高危

  检查业务命名规则：
    如果文件名明显像接口扫描或漏洞探测：
      标记为可疑

输出：
  高危清单
  可疑清单
  误报候选
  建议删除清单
```

---

## 10. 总结

上传目录安全的核心原则是：

- 不信任用户上传的文件名、后缀和 `Content-Type`。
- 使用魔数和解码能力判断真实文件类型。
- 静态目录永远不要具备脚本执行能力。
- 恶意文件清理后必须复扫。
- 文档记录安全知识时也要避免保存真实木马样本，防止杀毒误报。
